# CI, merge checks, and local equivalents

Issue #6 establishes two least-privilege GitHub Actions workflows. They run on every pull request and on pushes to `main`; neither workflow uses `pull_request_target`, repository secrets, signing credentials, or write permissions.

## Stable required checks

Configure the `main` branch ruleset to require these exact check names after the first PR run creates them:

| Required check              | Workflow                                                                                                                          | Local equivalent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Shared`                    | Linux: TypeScript, lint, format, all Jest, migrations, production persistence/recovery, Expo Doctor, npm audit                    | `npm run typecheck && npm run lint && npm run format:check && npm test && npm run test:persistence-migrations && npm run test:persistence-production && npm run doctor && npm run audit:ci`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `Contracts & privacy`       | Linux: fixture provenance, workflow policy, contract/state/logging rejection tests                                                | `npm run test:fixtures && npm run test:workflows && npm test -- --runTestsByPath __tests__/contracts.test.ts __tests__/versionedContracts.test.ts __tests__/stateMachines.test.ts __tests__/fixtureCorpus.test.ts __tests__/safeLogger.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Android`                   | Linux: app/native JVM tests, app/native lint, unsigned debug APK, API 24/34/35 instrumentation                                    | `./android/gradlew -p android --no-daemon :app:testDebugUnitTest :context-native:testDebugUnitTest :app:lintDebug :context-native:lintDebug :app:assembleDebug && "${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager" "emulator" "system-images;android-24;default;x86_64" && scripts/run-android-api24-instrumentation.sh && ./android/gradlew -p android --no-daemon -PreactNativeArchitectures=x86_64 :context-native:ciApi34DebugAndroidTest :context-native:ciApi35DebugAndroidTest`                                                                                                                                                                                                                                                                               |
| `iOS app & Share Extension` | macOS: shared Jest, workspace-independent podspec checksum regression, media fixture XCTest, native XCTest, clean-Pods Expo build | `npm test -- --ci && bundle exec ruby -rlogger -rcocoapods scripts/verify-podspec-checksum.rb && ruby -rfileutils -e "FileUtils.remove_dir('ios/Pods') if Dir.exist?('ios/Pods')" && bundle exec ruby -rlogger -S pod install --project-directory=ios --deployment && git diff --exit-code -- ios/Podfile.lock ios/AIContextPack.xcodeproj/project.pbxproj ios/AIContextPack/PrivacyInfo.xcprivacy && swift test --package-path modules/context-native/ios && ruby -rfileutils -e "FileUtils.remove_dir('ios/Pods') if Dir.exist?('ios/Pods')" && npm run ios -- --no-bundler --no-build-cache --device generic --output ./build/ci-expo-ios && git diff --exit-code -- ios/Podfile.lock ios/AIContextPack.xcodeproj/project.pbxproj ios/AIContextPack/PrivacyInfo.xcprivacy` |
| `Dependency review`         | Linux: new vulnerability/license review on pull requests                                                                          | GitHub dependency graph comparison plus the PR dependency checklist                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

Enable “Require branches to be up to date before merging,” require conversation resolution and independent approval, block force pushes/deletion, and do not permit bypass for implementation PRs. The repository administrator must apply the ruleset after the first workflow run because GitHub only offers check names that have already reported.

## Pinned toolchains and caches

- Node `22.13.1` comes from `.nvmrc`; npm `10.9.2` comes from `package.json#packageManager`. Workflows assert both exact versions before install.
- Android uses Eclipse Temurin `17.0.19+10` and the committed Gradle `9.3.1` wrapper. The required Android job first launches the minimum supported API 24 x86_64 AOSP image directly with KVM and snapshots disabled, waits for a completed boot, and verifies the device API. The API 24 package manager cannot reliably accept the large self-contained library test APK through ddmlib's split-install stream, so the runner builds the same instrumentation APK, installs a complete local file with `adb install --no-streaming`, and invokes its configured AndroidX runner fail-closed. It runs the complete suite plus the pre-35 PDF fallback explicitly and saves both results in the uploaded report tree. This avoids both the Gradle Managed Device snapshot-creation timeout and the old split-install timeout while making minimum-version compatibility a real required gate. The job then runs every `androidTest` on the checked-in `ciApi34` and `ciApi35` x86_64 AOSP Gradle Managed Devices: API 34 proves the rendered PDF fallback on the last API before the behavior boundary, while API 35 proves embedded-text detection; all three versions also cover contract parity, Inbox scanner, and handoff durability. The emulator package and all three exact x86_64 images are installed together before any emulator starts, so no test task depends on an in-process SDK download. AGP 8.12 also exposes `testedAbi` in the managed-device DSL without propagating it to generated setup tasks, so the narrowly matched `ciApi34Setup` and `ciApi35Setup` tasks repeat the same x86_64 pin; this prevents AGP 9's announced default from silently changing the CI architecture. Node/npm and Gradle caches are keyed by their lock/build inputs and toolchain selected by the setup actions.
- macOS uses the `macos-26` image with Xcode `26.6`, Ruby `3.4.9`, Bundler `2.6.9`, `Gemfile.lock`, and `ios/Podfile.lock`. Xcode 26.6 is the tested SDK 57 baseline and satisfies ADR 0001's Xcode 26.4+ requirement; Xcode 26.3 fails while compiling `expo-modules-jsi` because its Swift compiler cannot disambiguate the package's `abs(Double)` call. CocoaPods commands preload Ruby's standard `logger` library because ActiveSupport 6.1 expects it while security-patched `concurrent-ruby` no longer loads it transitively. The iOS npm script exports an absolute `BUNDLE_GEMFILE` and puts a checked-in shim first on `PATH`; that shim loads `pod` from the bundle even if Expo's own Bundler probe silently falls back to a global install after changing its child process working directory to `ios/`. CI compares the shim version with the locked version before testing. It caches only CocoaPods downloads, never the generated `ios/Pods` tree: the deployment install must reproduce the committed lock from no Pods, then the job removes that generated tree and builds through the supported `npm run ios` entry point. A second tracked-file diff proves Expo did not silently rewrite the lock or native integration files. The download-cache key includes Xcode/Ruby plus all JavaScript/Ruby/Pod locks; DerivedData is never cached.
- Every third-party action is pinned to a full commit SHA and annotated with the reviewed release version.

## Fixtures and artifacts

`fixtures/README.md` and `fixtures/provenance.json` are authoritative. CI verifies an exact inventory and SHA-256 for every synthetic fixture. Reports, unsigned debug products, and coverage are retained for seven days. CI must never upload Inbox/Application Support content, provider URIs, device captures, signing values, or any fixture derived from a user.

## v0.1 structured verification policy

`governance/v01-verification-policy.json` is the canonical machine-executable v0.1 device-verification policy. Its Draft 2020-12 schema is `schemas/governance/v1/v01-verification-policy-v1.schema.json`; both files have pinned SHA-256 digests in the verifier. The existing development-only Ajv pin validates the manifest. No network, registry metadata, locale, POS model, lexical database, or issue-prose interpretation participates in the policy result.

`npm run test:v01-verification-policy` runs synthetic manifest, integrity, inventory, diagnostic, input-bound, determinism, and prose-invariance tests, then validates the canonical files. The CLI bounds policy files to 64 KiB and stdin to 1 MiB and emits only stable rule IDs plus safe issue-number/field context. Free-form issue prose is descriptive, not the machine-executable source of verification policy. The canonical structured manifest and validated GitHub labels define whether physical-device evidence is a release gate.

For the live audit, pipe the read-only REST transport envelope into `node scripts/verify-v01-virtual-verification-policy.mjs --issues-stdin`. The verifier projects numbers 2–24, requires exactly Epic #2 and Issues #3–#24, validates non-empty title/body fields and labels, rejects in-range pull requests and duplicate/malformed records, and rejects `test:device-required` case-insensitively to match GitHub label identity. Out-of-range REST records do not enter the canonical inventory. Content is never printed.

## Dependency gate

The dependency-review action fails newly introduced high/critical vulnerabilities and denies AGPL/GPL licenses. Every dependency diff must also complete the PR template's New Architecture/Expo compatibility, maintenance, license, privacy/network, binary-size, and platform-impact review. Run Expo Doctor after JavaScript/native dependency changes; do not force dependency fixes across locked React Native/Expo ranges.

`expo` 57.0.11, `expo-dev-client` 57.0.10, and `expo-image-picker` 57.0.8 remain at the exact SDK 57 pins already accepted on `main`. Expo Doctor's registry-backed compatibility metadata can recommend newer compatible patches without a repository change; `expo.install.exclude` therefore excludes only these three reviewed pins from that dynamic version comparison while every other Doctor check remains enabled. Their alignment is tracked by dependency Issue [#47](https://github.com/phead198708/ai-context-pack/issues/47) and requires clean lock, iOS, and Android evidence rather than being mixed into a feature PR. The exclusion does not affect installation, autolinking, runtime behavior, permissions, analytics, networking, or imported-content handling.

`@expo/cli` 57.0.13 is an exact direct development pin for the SDK 57 build entry point already used by `npm run ios` and `npm run android`. `@expo/metro-config` 57.0.7 is an exact optional root lock pin but remains consumed only through the supported `expo/metro-config` sub-export; Expo also requires the same package transitively, so the build does not depend on optional-install success. This representation keeps Expo Doctor's supported dependency surface while making npm's audit directness and lock placement deterministic. Both are maintained Expo MIT packages (approximately 4.9 MB and 996 KB unpacked) and are excluded from the release runtime; the pins remove ambiguous boolean fix metadata without introducing a permission, analytics, backend, imported-content, or native-binary path. The React Native Community CLI packages move from 20.1.0 to the compatible 20.2.0 patch (the core CLI is approximately 229 KB unpacked, MIT, and maintained), removing the prior `fast-xml-parser` advisory path while preserving RN 0.86 tooling. The `xcode` dependency alone is overridden to maintained MIT `uuid` 11.1.1 (approximately 134 KB unpacked), which preserves the UUID API used for project generation and removes the vulnerable UUID 7 build-tool path. These changes are development/build-time only and require Expo Doctor plus clean iOS and Android builds.

### Vendored `image-size` security fork

GitHub currently lists no patched npm release for `GHSA-w3rx-r6r6-pgpr` or `GHSA-5p2g-fcmc-qvqq`; every published `image-size` version through 2.0.2 is affected. The repository therefore vendors the exact MIT-licensed `image-size@1.2.1` runtime as `@aicp/image-size` under `vendor/image-size`, keeps its 1.x CommonJS API for `metro@0.84.4`, and installs that local package at the `image-size` dependency name through an exact root file pin plus npm override. The 56-file copy occupies approximately 236 KB in the working tree and adds no native binary, permission, analytics, network path, or release-app parser. Metro uses it only for repository build assets; imported user content remains in the native/shared application pipeline.

The fork rejects BMFF boxes smaller than their eight-byte header before JXL/HEIF lookup, rejects JXL partial-stream boxes smaller than twelve bytes, and rejects ICNS entries that cannot advance or exceed the declared file boundary. `verify-vendored-image-size.mjs` pins the complete vendor-tree SHA-256, package identity, Metro and queue edges, registry integrity values, the single local `node_modules/image-size` link, and the absence of alternate registry copies. It also runs crafted ICNS, JXL, and HEIF payloads in a timeout-bounded child process so a regression cannot hang CI. `npm run audit:ci` executes that verifier first and then requires npm's version-2 report to contain zero findings at every severity and zero exceptions; nested high/critical advisories, malformed/oversized reports, count drift, or any nonempty inventory fail closed. `npm run test:npm-audit-policy` covers both verifiers and their drift cases. Replace the fork with a compatible maintained upstream release as soon as one exists, rerun Expo Doctor and clean platform builds, and remove the local package only after the lock proves Metro no longer resolves a vulnerable copy.

`expo-doctor` 1.20.1 is an exact development-only pin from the maintained Expo monorepo under the MIT license. It is the SDK configuration diagnostic already required by this CI gate; locking it prevents registry changes from altering a check without a repository diff. It contains no native runtime code, is compatible with the pinned Node 22.13.1 toolchain, and does not change New Architecture support or the iOS/Android app binaries. Its approximately 1.0 MB unpacked CLI is installed only for development/CI; it inspects project configuration and dependency metadata, never imported user content, and adds no product permission, analytics, or runtime network path.

`yaml` 2.9.0 is an exact development-only direct pin to the maintained `eemeli/yaml` parser under the ISC license. The workflow policy uses it to parse repository-owned permission mappings so quoted, inline, and job-level scalars cannot bypass least-privilege checks. It is pure JavaScript, already present in the locked development graph, approximately 686 KB unpacked, compatible with pinned Node 22.13.1, and adds no native/app binary, New Architecture, permission, analytics, imported-content, or runtime network impact.

`ajv` 8.20.0 is an exact development-only direct pin to the `ajv-validator/ajv` JSON Schema validator under the MIT license. At the 2026-08-05 audit, npm reports 8.20.0 as the latest release and a 2026-04-24 registry update, so the pin is on the maintained release line. Contract tests compile the six repository-owned Draft 2020-12 schemas with it; no production source imports the package. The pinned npm artifact is approximately 1.0 MB unpacked. It is pure JavaScript, so React Native New Architecture compatibility and native iOS/Android binary impact are not applicable; it is excluded from app bundles, adds no permission, analytics, backend, imported-content access, or runtime network path, and runs only against synthetic/repository contract data in development and CI.

`ajv-formats` 3.0.1 is an exact development-only direct pin to the `ajv-validator/ajv-formats` companion package under the MIT license. At the 2026-08-05 audit, npm reports 3.0.1 as both the pinned and latest release, last updated 2024-03-30; the older stable release cadence is accepted because it declares Ajv 8 compatibility and is isolated to tests. It adds standards-based date-time format validation to the same synthetic contract-test schemas. The pinned npm artifact is approximately 57 KB unpacked. It is pure JavaScript and test-only, so New Architecture compatibility and native iOS/Android binary impact are not applicable; it is excluded from app bundles and adds no permission, analytics, backend, imported-content access, or runtime network path.

## Intentional-regression proof

Before Phase 0 promotion, record a temporary, uncommitted regression for each platform and prove its required check fails:

1. Shared/contracts/privacy: alter a fixture byte or add `filename` to a safe logger record; `npm run test:fixtures` or the logging tests must fail before any sink is called.
2. Android: make one `PrivacySafeLoggerTest` assertion false; `:context-native:testDebugUnitTest` must fail.
3. iOS: make one `PrivacySafeLoggerTests` assertion false; `swift test --package-path modules/context-native/ios` must fail.

Restore the temporary edit, rerun the clean command, and include both expected-failure and clean-pass results in the PR. Never commit the intentional regression.
